apiVersion: apps/v1
kind: Deployment
metadata:
  name: loadtest
spec:
  replicas: 1
  selector:
    matchLabels:
      loadtest: docker
  template:
    metadata:
      labels:
        loadtest: docker
    spec:
      containers:
        - command:
            - sleep
            - "1000000000"
          image: eu.gcr.io/gardener-project/gardener/ops-toolbelt
          imagePullPolicy: Always
          name: dummy
          resources: { }
          stdin: true
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
      priority: 0
      nodeName: ip-10-240-7-228.eu-west-1.compute.internal
      restartPolicy: Always

